const root = document.querySelector('#root');

root.innerHTML = `
  <main class="app">
    <section class="panel">
      <span class="badge">Already built</span>
      <h1>Built React app</h1>
      <p>Publish the dist directory. No compute build step is needed.</p>
    </section>
  </main>
`;
